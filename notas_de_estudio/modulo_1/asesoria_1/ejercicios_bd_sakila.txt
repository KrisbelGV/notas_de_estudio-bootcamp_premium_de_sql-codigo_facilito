-- Consulta solo titles de los films:
SELECT title FROM film;

-- Consulta pel�culas por distintas duraciones, menos de 90 mins y m�s de 160 mins:
SELECT title, length FROM film WHERE length < 90 OR length > 160;

-- Ordena las pel�culas por su duraci�n y obten el top 10 m�s largos y el top 10 m�s cortas:
SELECT title, length FROM film ORDER BY length DESC LIMIT 10;
SELECT title, length FROM film ORDER BY length LIMIT 10;