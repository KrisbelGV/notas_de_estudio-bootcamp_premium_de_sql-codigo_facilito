Modulo: #1.
Clase: #3.
T�tulo: SQL b�sico.
Profesor: Keyla Dolores.
Estudiante: KrisbelGV.

Recursos:
	Clase grabada: https://codigofacilito.com/videos/introduccion-sql-basico
	Google drive: https://drive.google.com/drive/folders/1EzLfcvwULIc7cBtDhZmNUyX2jndcsGCn

Breve repaso en puntos clave:
	SQL es un lenguaje declarativo y case-insensitive.
	Por convenci�n las palabras reservadas se escriben en may�sculas.
	El orden de los elementos en la consulta es estricto, m�s m�nimo constituida por SELECT y FROM.
	SELECT * en producci�n puede afectar el rendimiento.
	Principales sentencias (b�sico):
		SELECT Selecciona las columnas. * todos, en campos separados por comas.
		FROM Determina la tabla. Sigue su nombre.
		WHERE Filtra acorde a una condici�n.
		ORDER BY Ordena los resultados conforme columna(s). DESC ser� descendente, ascendente por defecto. 
		LIMIT Restringe el n�mero de filas retornadas.
	Forzar comparaci�n exacta entre cadenas (sensible a may�sculas y min�sculas): COLLATE utf8mb4_bin
	Operadores de comparaci�n:
		= Igual.
		!= diferente.
		> Mayor que.
		< Menor que.
		BETWEEN Rango de valores.
		LIKE B�squeda de patrones.
		IN() M�ltiples valores.
	Alias (apodos temporales):
		Para columnas: columna AS alias.
		Para tablas: tabla alias.
	Comentarios:
		Una sola l�nea: --
		Varias l�neas: /**/
	Comodines:
		% Remplaza cero o m�s caracteres.
		_ Remplaza un car�cter.
	Siglas DDL: Data Definition Lenguage (en espa�ol �Lenguaje de Definici�n de Datos�).
	Engloba toda clausula relativo a la estructura de la BD.
	Sus cambios son permanentes.
	Principales sentencias (DDL):
		CREATE Crea BBDD, tablas, �ndices, etc.
		DROP Elimina BBDD, tablas, columnas, �ndices, etc.
		ALTER Modifica la estructura de una tabla.
	Crear tabla:
		CREATE TABLE tabla (
			columna1 TIPO,
			columna 2 TIPO�
			columnaN TIPO
		);
	Manipular columnas:
		Crear: ALTER TABLE tabla ADD COLUMN columna TIPO;
		Modificar tipo de dato: ALTER TABLE tabla ALTER COLUMN columna TIPO;
		Eliminar: ALTER TABLE tabla DROP COLUMN columna; No soportado por algunos motores (como SQLite).
	Eliminar tablas:
		DROP TABLE tabla; Eliminar� la estructura y los datos.
	Eliminar solo filas:
		TRUNCATE TABLE tabla;
	Siglas DML: Data Manipulation Language (en espa�ol �Lenguaje de Manipulaci�n de Datos�).
	Considera las sentencias en lo que concierne a los datos de la BD.
	Pueden deshacerse mediante el uso de transacciones.
	Principales sentencias (DDL):
		INSERT Agrega nuevos registros.
		UPDATE Modifica registros existentes.
		DELETE Elimina registros espec�ficos.
	Insertar datos:
		INSERT INTO tabla (columna1, columna 2� columnaN) VALUES (valor1, valor2� valorN); La especificaci�n de columnas puede obviarse siguiendo su orden en la tabla, (mejor practica indicarlos).
	Modificar datos:
		UPDATE tabla SET columna1 = valor, columna2 = valor� columnaN = valor; Se usa WHERE para especificar que filas cambiar, su omisi�n actualizar� todo (dependiendo el motor y su configuraci�n).
	Eliminar datos:
		DELETE FROM tabla; Sin un WHERE igualmente eliminar� todo registro.
	Siglas CRUD: Create, read, update, delete (en espa�ol crear, leer, actualizar y eliminar).
	Conjunto de cuatro operaciones fundamentales para manipular datos.
	DML es el subconjunto de SQL que implementa CRUD.
	Desactivar configuraci�n que restringe cambios: SET SQL_SAFE_UPDATES = 0
	MDF y LDF son archivos de guardado con la informaci�n y cambios realizados en la DB (en SQL Server).

Notas al pie:
	El archivo con los scripts (para MySQL) se encuentra adjunto en este mismo directorio, as� como el ejercicio pr�ctico final.